<?xml version="1.0" encoding="utf-8"?>

<!DOCTYPE suppressions PUBLIC
  "-//Checkstyle//DTD SuppressionFilter Configuration 1.0//EN"
  "https://checkstyle.org/dtds/suppressions_1_0.dtd">

<suppressions>
  <!-- char[] to String -->
  <suppress files="NPToolArchives.java"
            checks="IllegalInstantiation"/>

  <!-- Lacking pattern matching. -->
  <suppress files="NPA1Validation.java"
            checks="NPathComplexity"/>
  <suppress files="NPA1Validation.java"
            checks="CyclomaticComplexity"/>

  <!-- Lacking pattern matching. -->
  <suppress files="NPU1Validation.java"
            checks="NPathComplexity"/>
  <suppress files="NPU1Validation.java"
            checks="CyclomaticComplexity"/>
  <suppress files="NPU1Validation.java"
            checks="JavaNCSS"/>
  <suppress files="NPU1Validation.java"
            checks="ExecutableStatementCount"/>
  <suppress files="NPU1Validation.java"
            checks="ClassFanOutComplexity"/>

  <!-- Lacking pattern matching. -->
  <suppress files="NPTX1Serializer.java"
            checks="NPathComplexity"/>

  <!-- Lacking pattern matching. -->
  <suppress files="NPTXChecker.java"
            checks="NPathComplexity"/>

  <!-- Lacking pattern matching. -->
  <suppress files="NPSC1File.java"
            checks="NPathComplexity"/>

  <!-- Lacking pattern matching. -->
  <suppress files="NPTXEvaluator.java"
            checks="NPathComplexity"/>
  <suppress files="NPTXEvaluator.java"
            checks="CyclomaticComplexity"/>

  <!-- Lacking pattern matching. -->
  <suppress files="NPUCmd.java"
            checks="NPathComplexity"/>
  <suppress files="NPUCmd.java"
            checks="ClassDataAbstractionCoupling"/>
  <suppress files="NPUCmd.java"
            checks="CyclomaticComplexity"/>
  <suppress files="NPUCmd.java"
            checks="ExecutableStatementCount"/>

  <!-- char[] -> String -->
  <suppress files="NPTX1Statements.java"
            checks="IllegalInstantiation"/>

  <!-- Lots of injected services. -->
  <suppress files="NPAgentService.java"
            checks="ParameterNumber"/>

  <!-- Lots of injected services. -->
  <suppress files="NPAssignmentTask.java"
            checks="ParameterNumber"/>

  <!-- Manually constructed strings. -->
  <suppress files="NPUCmdPlanValidate.java"
            checks="IllegalInstantiation"/>
  <suppress files="NPUCmdPlanPut.java"
            checks="IllegalInstantiation"/>
  <suppress files="NPDBQPlanGetRaw.java"
            checks="IllegalInstantiation"/>

</suppressions>
